# ===========================================# ===========================================

# Rotation Ready - Backend Environment Config# Rotation Ready - Backend Environment Config

# ===========================================# ===========================================

# Copy this file to .env and fill in the values# Copy this file to .env and fill in your values



# Application# Application

FLASK_ENV=productionFLASK_ENV=development

SECRET_KEY=<generate-with: python -c "import secrets; print(secrets.token_hex(32))">SECRET_KEY=your-secret-key-change-in-production



# Database# Database (SQLite for development)

# For development (SQLite): leave commented# For production, use PostgreSQL:

# For production (PostgreSQL): Render provides this automatically# DATABASE_URL=postgresql://user:password@localhost:5432/rotationready

# DATABASE_URL=postgresql://user:password@localhost:5432/rotationready

# JWT Configuration

# JWT ConfigurationJWT_SECRET_KEY=dev-secret-key-change-in-production

JWT_SECRET_KEY=<generate-with: python -c "import secrets; print(secrets.token_hex(32))">

# CORS

# CORS - Add your frontend URLs (comma-separated)CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Development: http://localhost:5173

# Production: https://your-app.vercel.app# ===========================================

CORS_ORIGINS=http://localhost:5173# Email Configuration (Flask-Mail)

# ===========================================

# ===========================================

# Email Configuration (Flask-Mail)# Set to 'true' to enable actual email sending

# ===========================================# When 'false', emails are printed to console (development mode)

MAIL_ENABLED=false

# Set to 'true' to enable actual email sending

MAIL_ENABLED=false# SMTP Server Settings

MAIL_SERVER=smtp.gmail.com

# SMTP Server SettingsMAIL_PORT=587

MAIL_SERVER=smtp.gmail.comMAIL_USE_TLS=true

MAIL_PORT=587MAIL_USE_SSL=false

MAIL_USE_TLS=true

MAIL_USE_SSL=false# SMTP Authentication

# For Gmail, use an App Password (not your regular password)

# SMTP Authentication# Generate at: https://myaccount.google.com/apppasswords

# For Gmail, use an App Password (not your regular password)MAIL_USERNAME=

# Generate at: https://myaccount.google.com/apppasswordsMAIL_PASSWORD=

MAIL_USERNAME=your-email@gmail.com

MAIL_PASSWORD=your-app-password# Sender Information

MAIL_DEFAULT_SENDER=noreply@rotationready.com

# Sender InformationMAIL_DEFAULT_SENDER_NAME=Rotation Ready

MAIL_DEFAULT_SENDER=your-email@gmail.com

MAIL_DEFAULT_SENDER_NAME=Rotation Ready# Debug/Development Options

MAIL_DEBUG=false

# Debug/Development OptionsMAIL_SUPPRESS_SEND=false

MAIL_DEBUG=false
MAIL_SUPPRESS_SEND=false
