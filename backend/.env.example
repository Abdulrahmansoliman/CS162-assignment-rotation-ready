# ===========================================
# Rotation Ready - Backend Environment Config
# ===========================================
# Copy this file to .env and fill in your values
# ===========================================
# Application
# ===========================================
# Set to 'development' or 'production'
FLASK_ENV=development
# Generate a secret key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-in-production
# ===========================================
# Database
# ===========================================
# For development (SQLite): leave DATABASE_URL commented out
# For production (PostgreSQL): uncomment and set the following
# DATABASE_URL=postgresql://user:password@localhost:5432/rotationready
# ===========================================
# JWT Configuration
# ===========================================
# Generate a JWT secret key: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
# ===========================================
# CORS
# ===========================================
# Add your frontend URLs (comma-separated)
# Development: http://localhost:5173
# Production: https://your-app.vercel.app
CORS_ORIGINS=http://localhost:5173
# ===========================================
# Email Configuration (Flask-Mail)
# ===========================================
# Set to 'true' to enable actual email sending.
# When 'false', emails are printed to console (development mode).
MAIL_ENABLED=false
# SMTP Server Settings
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USE_SSL=false
# SMTP Authentication
# For Gmail, use an App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
# Sender Information
MAIL_DEFAULT_SENDER=your-email@gmail.com
MAIL_DEFAULT_SENDER_NAME=Rotation Ready
# Debug/Development Options
MAIL_DEBUG=false